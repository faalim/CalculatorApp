we need JDK because we use java 

Gradle builds APK (Android Application package) <-- contains Java code, external resources, images/videos 
	sent to emulator or send to physical device or send to 	google play 

Gradle keeps running in backgrouond 
	process of compiling and running 

SDK Software Development kit <-- everything that we need the andoid to work in this package 
	when 
Logcat <-- command line tool for messages 
AndroidX<-- makes librarys that takes in account naming 
manifest <-- like info in swift

Activity <-- means user interface 
minimum SDK stays 

Each SDK has diffrent features that only some ppl 

can use nugget but cant use marshmallow 

API 30